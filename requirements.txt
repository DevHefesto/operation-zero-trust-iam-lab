# =============================================================================
# Operation Zero Trust — Dependências
# =============================================================================
# Instale com: pip install -r requirements.txt

# Microsoft Authentication Library — gerencia tokens OAuth 2.0 / OIDC
msal==1.31.1

# HTTP client para chamadas à Microsoft Graph API
requests==2.32.3

# Carrega variáveis do arquivo .env com segurança
python-dotenv==1.1.0

# Formatação de tabelas no terminal (exibição de usuários e relatórios)
tabulate==0.9.0
